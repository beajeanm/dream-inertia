(library
 (name example_lib)
 (libraries dream))

(rule
 (targets assets.ml)
 (deps
  (source_tree ../../dist/))
 (action
  (with-stdout-to
   %{null}
   (run %{bin:ocaml-crunch} -m plain ../../dist/ -o assets.ml))))
