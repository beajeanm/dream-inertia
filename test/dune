(tests
 (names test)
 (libraries
  dream
  dream_inertia
  base
  yojson
  lwt
  lwt.unix
  alcotest
  alcotest-lwt)
 (preprocess
  (pps lwt_ppx)))

(rule
 (targets index.ml)
 (deps index.eml.html)
 (action
  (run dream_eml %{deps} --workspace %{workspace_root})))
